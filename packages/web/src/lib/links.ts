/**
 * External links surfaced in onboarding. `EXTENSION_URL` is where "Get the
 * extension" points — set this to the published store listing once it exists;
 * for now it's the repo (load-unpacked instructions live there).
 */

export const EXTENSION_URL = "https://github.com/kasusoba/sixseven#extension";
